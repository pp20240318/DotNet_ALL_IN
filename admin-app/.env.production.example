# Production build: set API base URL to your deployed Admin API.
# When served behind nginx (docker-compose), use relative path so nginx proxies /__api.
VITE_API_BASE_URL=/__api

# Only used by `pnpm preview` locally; nginx handles proxy in Docker.
VITE_API_PROXY_TARGET=http://localhost:8080
